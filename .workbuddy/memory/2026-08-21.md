# 2026-08-21 工作日志

## 版本与基线核对（电磁干扰 Agent Benchmark）

- 用户用百炼 `deepseek-v3.2` 跑了 run8、用官方 `deepseek-chat` 跑了 run9（均 50 样本，无推理）。
- **确认 ds_run2~6 确为 v3.2**：run8(百炼v3.2) 与 run9(官方chat=v3.2) 指标几乎重合，且 ds_run2~6
  mtime 为 8/16（"几天前"）处 v3.2 时代。版本混淆（confound）消除。
- **调制跃升归因纠正**：run6 调制 0.11 → run8/run9 调制 0.33~0.36 是因为用户在 run6 之后
  优化了调制识别（代码/提示），**非模型版本差异**（run6 与 run9 同为官方 chat）。
  ⚠️ 该优化是权衡：参数估计变好（频偏MAE 0.026→0.016、带宽MAE 0.70→0.41、调制 0.11→0.33），
  但源数/类别/DOA/漏检变差（0.76→0.66、0.50→0.38、0.58→0.48、漏检32→40）。
- **文件整理**：删除旧 run7（百炼初代 v3 错版本、DOA MAE 8.36° 离群），将 run8(百炼v3.2) 改名 run7。
  最终：ds_run2~6（官方chat/v3.2 优化前，8/16）、ds_run7（百炼v3.2 优化后）、ds_run9（官方chat/v3.2 优化后）。
  注：编号在 run7 与 run9 间有 gap（无 run8），如需连续可把 run9 改名 run8。
